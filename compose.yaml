services:
  uptime-kuma:
    image: ghcr.io/evannotfound/uptime-kuma:edge
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    ports:
      # <Host Port>:<Container Port>
      - "3001:3001"
